# Raspberry Pi Connection Settings
# SSH username for Raspberry Pi
PI_USER=prouty

# Raspberry Pi hostname or IP address (without username)
PI_HOSTNAME=raspberrypi.local

# Remote project directory on Raspberry Pi
PI_DIR=/home/prouty/projects/noisedetector

# Local Development Settings
# Local project directory (defaults to $HOME/projects/noisedetector)
LOCAL_DIR=${HOME}/projects/noisedetector

# Service Settings (for systemd service file)
# User to run systemd service as (defaults to PI_USER)
SERVICE_USER=prouty

# Working directory for systemd service (defaults to PI_DIR)
SERVICE_WORKING_DIR=/home/prouty/projects/noisedetector
